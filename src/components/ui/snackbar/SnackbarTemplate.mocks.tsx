import { ISnackbarTemplate } from './SnackbarTemplate';

const Snackbar: ISnackbarTemplate = {
  snackbarDuration: 500,
  snackbarMessage: '',
  alertColor: 'success',
  snackbarState: false,
};

export const mockSnackbarTemplateProps = {
  Snackbar,
};
