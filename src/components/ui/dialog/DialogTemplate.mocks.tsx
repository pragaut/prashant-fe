import { IDialogTemplate } from './DialogTemplate';

const base: IDialogTemplate = {
  dialogTitle: '',
  dialogContent: '',
  dialogActions: '',
  dialogState: false,
};

export const mockDialogTemplateProps = {
  base,
};
